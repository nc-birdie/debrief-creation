# Optional: Set a shared database path so all team members access the same data.
# Point this to a network drive, cloud-synced folder, or any shared location.
# If not set, defaults to ./prisma/dev.db (local only).
# DATABASE_PATH=//server/shared/debrief/dev.db
# DATABASE_PATH=C:/Users/Shared/debrief/dev.db
